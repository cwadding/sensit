require "spec_helper"

module Sensit
    describe DevicesController do
      describe "routing" do
        # it "routes to #index" do
        #   get("/api/devices"}.should route_to("sensit/api/devices#index")
        # end

        # it "routes to #show" do
        #   get("/api/devices/1").should route_to("sensit/api/devices#show", :id => "1")
        # end

        # it "routes to #create" do
        #   post("/api/devices").should route_to("sensit/api/devices#create")
        # end

        # it "routes to #update" do
        #   put("/api/devices/1").should route_to("sensit/api/devices#update", :id => "1")
        # end

        # it "routes to #destroy" do
        #   delete("/api/devices/1").should route_to("sensit/api/devices#destroy", :id => "1")
        # end

      end
    end
end
