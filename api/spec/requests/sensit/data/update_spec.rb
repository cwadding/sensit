require 'spec_helper'
describe "PUT sensit/data#update" do
# //PUT /nodes/:node_id/topics/:topic_id/feeds/:feed_id/fields/:key
# {
#    "value":12
# }
	# before(:each) do
	# 	@node = FactoryGirl.create(:complete_node)
	# 	@topic = @node.topics.first
	# 	@feed = @topic.feeds.first
	# 	@data = @feed.data_rows.first
	# end
 #  it "" do
 #  	params = {}
 #    put "/api/nodes/#{@node.id}/topics/#{@topic.id}/feeds/#{@feed.id}/data/#{@data.id}", valid_request(params), valid_session

 #  end
end