require 'spec_helper'
describe "GET sensit/data#index" do

	# before(:each) do
	# 	@node = FactoryGirl.create(:complete_node)
	# 	@topic = @node.topics.first
	# 	@feed = @topic.feeds.first
	# end
 #  it "" do
 #  	params = {}
 #    get "/api/nodes/#{@node.id}/topics/#{@topic.id}/feeds/#{@feed.id}/data", valid_request(params), valid_session

 #  end
end