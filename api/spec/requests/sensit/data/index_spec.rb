require 'spec_helper'
describe "GET sensit/data#index" do

	# before(:each) do
	# 	@topic = FactoryGirl.create(:topic_with_feeds_and_fields)
	# 	@feed = @topic.feeds.first
	# end
 #  it "" do
 #  	params = {}
 #    get "/api/topics/#{@topic.id}/feeds/#{@feed.id}/data", valid_request(params), valid_session

 #  end
end