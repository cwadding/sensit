require 'spec_helper'
describe "POST sensit/data#create" do

	# before(:each) do
	# 	@topic = FactoryGirl.create(:topic_with_feeds_and_fields)
	# 	@feed = @topic.feeds.first
	# end
	# it "" do
	# 	params = {}
	# 	post "/api/topics/#{@topic.id}/feeds/#{@feed.id}/data", valid_request(params), valid_session
	# end
end