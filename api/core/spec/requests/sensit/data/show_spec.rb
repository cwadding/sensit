require 'spec_helper'
describe "GET sensit/data#show" do

	# before(:each) do
	# 	@topic = FactoryGirl.create(:topic_with_feeds)
	# 	@feed = @topic.feeds.first
	# 	@values = @feed.values.first
	# end
 #  it "" do
 #  	params = {}
 #    get "/api/topics/#{@topic.id}/feeds/#{@feed.id}/data/#{@data.id}", valid_request(params), valid_session

 #  end
end